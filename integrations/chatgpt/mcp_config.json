{
  "name": "graft",
  "description": "Persistent graph memory for the assistant. Use BEFORE solving non-trivial problems and AFTER novel solutions. Multi-tenant via profiles. Supports search (query/retrieve/explore), save (classify+insert), maintenance (delete), profile management (list/add/remove/export/import/merge), and analytics.",
  "transport": {
    "type": "stdio",
    "command": "python",
    "args": [
      "C:/personal/Graft/integrations/mcp-server/server.py"
    ],
    "env": {
      "GRAFT_BIN": "C:/Users/aredegalli/.graft/bin/graft.exe"
    }
  }
}
